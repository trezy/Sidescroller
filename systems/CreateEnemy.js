const CreateEnemy = (state, { touches, screen }) => {
  // const { world } = state['physics']
  // const boxSize = Math.trunc(Math.max(screen.width, screen.height) * 0.075)
  // const presses = touches.filter(touch => touch.type === 'press')

  // for (const press of presses) {
  //   const body = Matter.Bodies.rectangle(
  //     press.event.pageX,
  //     press.event.pageY,
  //     boxSize,
  //     boxSize,
  //     { frictionAir: 0.021 }
  //   )

  //   Matter.World.add(world, [body])

  //   state[uuid()] = {
  //     body: body,
  //     size: [boxSize, boxSize],
  //     color: 'pink',
  //     renderer: Box,
  //   }
  // }

  return state
}





export { CreateEnemy }
